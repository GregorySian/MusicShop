<a class="btn btn-block btn-secondary" 
   asp-action="List" 
   asp-controller="Product"
   asp-route-genre="">Home</a>

    @foreach (string genre in Model)
    {
        <a class="btn btn-block btn-secondary"
           asp-action="List"
           asp-controller="Product"
           asp-route-genre="@genre"
           asp-route-productPage="1">@genre</a>
    }